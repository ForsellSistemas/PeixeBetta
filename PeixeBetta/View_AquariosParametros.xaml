<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PeixeBetta.View_AquariosParametros"
             Title="Aquários e Parâmetros">

    <ContentPage.Resources>
        <Style TargetType="Button">
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="TextColor" Value="Black"/>
            <Setter Property="BackgroundColor" Value="GreenYellow"/>
        </Style>
    </ContentPage.Resources>

    <Grid Margin="10">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="0.5*" />
        <ColumnDefinition Width="0.5*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*" />
            <RowDefinition Height="0.5*" />
        </Grid.RowDefinitions>

        <Button   Text="Tipos de Aquários" />
        <Button  Grid.Column="1"  Text="Pedras e Decoração" />
        <Button Grid.Row="1" Text="Troca Parciao de Água (TPA)"/>
        <Button Grid.Row="1" Grid.Column="1"  Text="Parâmetros"/>

    </Grid>

</ContentPage>